@model IEnumerable<Master_Details_JQure.Models.City>
<br />
<br />

<h3 class="text-center bg-info text-white">City List</h3>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CityName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StateId)
            </th>
            <th>Action</th>
            <th>
                <a onclick="showInPopup('@Url.Action("AddorEdit","City",null,Context.Request.Scheme)','New City')" class="btn  btn-primary"><i class="fa-solid fa-plus"></i> State Country</a>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CityName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.State.StateName)
                </td>
                <td>
                    <a onclick="showInPopup('@Url.Action("AddorEdit","City",new{id=item.Id},Context.Request.Scheme)','New City')" class="btn btn-success"><i class="fa-solid fa-pen-to-square"></i> Edit</a> |
                    <form asp-action="Delete" asp-route-id="@item.Id" onsubmit="return jQueryAjaxDelete(this);" class="d-inline">
                        <input type="hidden" asp-route-id="@item.Id" />
                        <button type="submit" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Delete</button>
                    </form>

                </td>
            </tr>
        }
    </tbody>
</table>


<script src="~/js/site.js"></script>

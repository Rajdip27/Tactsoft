@model IEnumerable<Master_Details_JQure.Models.Item>

<br />
<br />

<h3 class="text-center bg-info text-white">Item List</h3>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discription)
            </th>

            <th>Action</th>
            <th>
                <a onclick="showInPopup('@Url.Action("AddorEdit","Item",null,Context.Request.Scheme)','New Item')" class="btn  btn-primary"><i class="fa-solid fa-plus"></i> State Country</a>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ItemCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ItemName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Discription)
                </td>
                <td>
                    <a onclick="showInPopupI('@Url.Action("AddorEdit","Item",new{id=item.Id},Context.Request.Scheme)','New Item')" class="btn btn-success"><i class="fa-solid fa-pen-to-square"></i> Edit</a> |
                    <form asp-action="Delete" asp-route-id="@item.Id" onsubmit="return jQueryAjaxDelete(this);" class="d-inline">
                        <input type="hidden" asp-route-id="@item.Id" />
                        <button type="submit" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Delete</button>
                    </form>

                </td>
            </tr>
        }
    </tbody>
</table>
